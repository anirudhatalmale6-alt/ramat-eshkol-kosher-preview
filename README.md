# Ramat Eshkol Kosher — live preview

A static export of the public page, published so it can be clicked through in a
real browser. Generated by `tools/export_preview.php` in the main repository:

https://github.com/anirudhatalmale6-alt/hashgacha-directory

The admin panel is not part of this export — it needs PHP, which GitHub Pages
does not run.
